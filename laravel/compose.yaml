version: "3.8"

services:
  web:
    image: nginx
    container_name: web_server
    ports:
      - "8000:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro

  # php:
  # mysql:
  # composer:
  # artisan:
  # npm:
